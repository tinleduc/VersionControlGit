It's All About the Index

File Classifications in Git

Using git add

Some Notes On Using git commit

Using git rm

Using git mv

A Note on tracking Renames

the .gitignore file

A Detail View of Git's Object Model
